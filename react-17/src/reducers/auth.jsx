const initialValue = {
  accessToken: "",
  errorMessage: "",
  expirationTime: "",
  expiresIn: "",
  isLogged: false,
  tokenType: "",
}

const auth = (state = initialValue, action) => {
  return state
}

export default auth;